name: 🔒 Analyse de sécurité avec Bandit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  bandit-scan:
    name: 🧠 Analyse statique du code avec Bandit
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Cloner le dépôt
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      # Étape 2 : Installer Python
      - name: 🐍 Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Étape 3 : Installer Bandit
      - name: 🔧 Installer Bandit
        run: |
          python -m pip install --upgrade pip
          pip install bandit

      # Étape 4 : Lancer l’analyse Bandit
      - name: 🔍 Exécuter Bandit sur le code
        run: |
          bandit -r . -f json -o bandit-report.json || true

      # Étape 5 : Vérifier les vulnérabilités de niveau HIGH
      - name: 🚫 Échec si vulnérabilités HIGH trouvées
        run: |
          high_count=$(cat bandit-report.json | jq '.results | map(select(.issue_severity=="HIGH")) | length')
          echo "Nombre de vulnérabilités HIGH détectées : $high_count"
          if [ "$high_count" -gt 0 ]; then
            echo "❌ Échec : des vulnérabilités HIGH ont été détectées !"
            exit 1
          else
            echo "✅ Aucun problème critique détecté."
          fi

      # Étape 6 : Sauvegarder le rapport en artefact
      - name: 📦 Sauvegarder le rapport Bandit
        uses: actions/upload-artifact@v4
        with:
          name: rapport-bandit
          path: bandit-report.json
